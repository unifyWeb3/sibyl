# services/kite

Kite primitive wrappers. Thin, well-typed SDK helpers consumed by the three agents.

Day 1 fills in:
- `client.ts` — shared RPC + AA SDK client
- `passport.ts` — Passport registration, lookup
- `session.ts` — Session creation with spending rules
- `x402.ts` — payment client + facilitator integration
- `attestation.ts` — sign + post attestations

Built after Milestone 1 clears.
